server:
  port: 8082

spring:
  application:
    name: core-service
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://ep-hidden-bar-azmsarvo-pooler.c-3.ap-southeast-1.aws.neon.tech/core_db?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_4NeM5xPfaAsY
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


openapi:
  service:
    title: Core Service API
    version: 1.0.0

logging:
  level:
    root: INFO
    web: INFO

springdoc:
  api-docs:
    path: /v3/api-docs

  swagger-ui:
    path: /swagger-ui.html

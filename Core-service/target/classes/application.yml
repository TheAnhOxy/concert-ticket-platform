server:
  port: 8082

spring:
  application:
    name: core-service
  profiles:
    active: dev

openapi:
  service:
    title: Core Service API
    version: 1.0.0

logging:
  level:
    root: INFO
    web: INFO

springdoc:
  api-docs:
    path: /v3/api-docs

  swagger-ui:
    path: /swagger-ui.html

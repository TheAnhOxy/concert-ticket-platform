server:
  port: 8085

spring:
  application:
    name: notification-service
  profiles:
    active: dev

openapi:
  service:
    title: Notification Service API
    version: 1.0.0

logging:
  level:
    root: INFO
    web: INFO

springdoc:
  api-docs:
    path: /v3/api-docs

  swagger-ui:
    path: /swagger-ui.html
